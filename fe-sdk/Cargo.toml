[package]
name = "fe-sdk"
version = "0.1.0"
edition = "2021"
description = "Stable extension SDK for FractalEngine plugins"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
