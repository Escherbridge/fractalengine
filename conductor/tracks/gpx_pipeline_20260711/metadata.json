{
  "id": "gpx_pipeline_20260711",
  "description": "GPX end-to-end pipeline: import UI -> parse -> petal-bound track/waypoint nodes -> terrain rendering -> gis/tracks endpoint",
  "type": "feature",
  "created_at": "2026-07-11T00:00:00Z",
  "status": "done",
  "execution_wave": 0,
  "wave_rationale": "User escalated 2026-07-11 ('gpx logic as well its pretty key'). GIS round-2 fleet (W-D + W-A UI surface).",
  "depends_on": [
    "petal_gis_endpoints_20260711"
  ],
  "blocks": [],
  "archived": true,
  "notes": "Phase 7 shipped GPX parsing + GpxTrackLine rendering; the import->persist->render->serve chain has never been wired e2e from the UI. Contract: fe-ui gpx_ops queue (mirrors asset_ops), bridge in main binary, gpx_type=track node properties, gpx_track layer name honored in petal_binding (W-B). | PRUNE 2026-07-14: verified shipped + working in code (feature present, tested); flipped stale status->done and archived."
}