---
type: Decision Register
title: Outstanding decisions — 2026-07-14/15 board hygiene + 3 implementation waves
tags: [decisions, register, ratification, outstanding_decisions_20260715]
timestamp: 2026-07-15T00:00:00Z
resource: ./metadata.json
---

# Outstanding Decision Register — 2026-07-14/15

Sources: 37-agent track audit (2026-07-14; 68 raw items → 53 deduped entries) + 72 provisional decisions logged by the 3 implementation waves (2026-07-14/15, commits 9ef76a1, f062921, 51bc90e). Wave decisions that refine an audit entry are merged into that entry's default with the implementing track cited; genuinely new calls got new numbers (D-54..D-67). Pure implementation-detail wave notes (no ratification value) are NOT registered — they live in the implementing tracks' plan.md / AGENTS.md dated notes.

**72 entries: 34 [USER], 38 [DEFAULTED].** Tags: **[USER]** = genuine product/money/scope-kill call — needs an actual decision. **[DEFAULTED]** = a sensible default was taken (audit pass or implementation wave); needs only ratification (see checklist at bottom). Track names drop their `_2026xxxx` suffix.

## A. BI egress shape, credentials, limits

- **D-1 BI egress shape** [DEFAULTED] — Which egress surface is primary for the PowerBI persona: parquet/CSV download, OData feed, or DuckDB native wire? Src: analytics_egress (plan D1), iot_spatial_reporting, roadmap §1. Default: parquet+CSV download endpoints with range support so DuckDB httpfs `read_parquet()` works; OData fast-follow; native wire out of scope. Wave (analytics_egress, iot_spatial): exports + parquet/csv share links are node-table-only for now (400 otherwise); IoT rows have no file-based export yet — /query JSON + signed URLs are the interim IoT egress; reading-shaped export.parquet/csv is iot plan Phase-4 polish, coordinate with egress Phase-6 e2e. Undecided: the P0 track's Phase 2 builds the wrong surface for the target persona.
- **D-2 Shareable-URL credential model** [DEFAULTED] — What credential does a query string pasted into PowerBI/spreadsheets embed? Src: analytics_egress (plan D2, FR-2/FR-6). Default: short-TTL signed scoped URL, not long-lived bearer tokens. Wave (analytics_egress): implemented with ed25519 (fe_identity NodeKeypair + verify_strict) rather than HMAC — no new secret type; parquet/csv mints additionally require a petal-scoped issuer token (enforced at mint and redemption); TTL clamped client-side to 86400 s max. Undecided: security posture of the flagship feature is unreviewed.
- **D-3 Query/export limit values** [DEFAULTED] — Row/byte caps for `/query` and export endpoints. Src: analytics_egress (plan D4). Default: 10k rows/8 MiB JSON; 500k rows/128 MiB exports; named constants in one module. Wave: exceeding a cap returns a clear "row cap exceeded" error, never a truncated result — BI tools must not silently see partial data. Undecided: unbounded exports = trivial DoS on the analytics backend.
- **D-4 RBAC-on-query-results owner** [DEFAULTED] — Does row/column filtering live in analytics_egress or auth_policy_pattern? Src: auth_policy_pattern, analytics_egress, roadmap hardening gaps. Default: minimal slice (scope ceiling + export pre-filter on `inject_scope_filter`) in egress; full policy-engine RBAC migrates to auth_policy later. Undecided: two tracks both/neither implement it.
- **D-5 IoT row storage** [USER] — SurrealDB live table vs columnar/parquet-backed view for ingested IoT rows (coupled to D-1; shapes query latency and egress). Src: iot_spatial_reporting FR-1. Wave (iot_spatial): v1 landed as an append-only SurrealDB `iot_reading` table (ULID ids); the columnar/parquet question now = the scale + file-egress path. Undecided: wrong pick at scale is expensive to reverse.
- **D-6 IoT v1 ingestion shape** [DEFAULTED] — Webhook receiver vs polling connector, and what auth the external surface uses. Src: iot_spatial_reporting FR-4. Wave (iot_spatial): landed as a push-style authenticated HTTP batch-ingest endpoint (DID token, Editor+; caps in D-58); polling connectors remain future work. Undecided: was [USER]; the wave's push default needs ratification.
- **D-7 node_log retention semantics** [USER] — How much history survives the cap, given node_log also feeds incremental P2P sync (data-loss vs memory). Src: iot_spatial_reporting FR-2 (feeds p2p_unblock_now FR-2). The cap *value* was defaulted (D-59); the semantics question — what a sync peer can still catch up on — is still open. Undecided: a cap tuned for memory silently breaks incremental sync.
- **D-8 BIM stats egress surface** [USER] — Expose primitive stats via fe-query SQL (copyable into PowerBI), ApiExtensionHandle HTTP routes, or both. Src: bim_primitives FR-8. Undecided: BIM reporting lands off the strategic egress path.
- **D-54 GeoParquet stack + column contract** [DEFAULTED] — Is the shipped encoding stack the v1 export contract? Src: analytics_egress + build_size_mobile_prep waves. Default: plain parquet+arrow 54.x with hand-written ISO WKB Point Z (geoarrow-rs rejected: churning writer surface, large dep tree), pinned to datafusion-46's arrow line, declared in fe-query only under a lighter `parquet` feature; rotation/scale flattened to six Float32 columns (BI/DuckDB-friendly); node_log NOT exported (audit log ≠ analytics); properties as one JSON-string column; CSV carries CRS as a leading `# crs=` comment + X-FE-CRS header; default egress SQL is guard-compatible `SELECT * FROM node WHERE ...`; bbox filters on position.coordinates[0]/[1] pending Phase-6 e2e validation; coords=latlon passes EntitySnapshot f32 (~1 m mid-latitude) — documented v1 limit. Undecided: BI consumers build against an unratified schema.
- **D-55 Share-link key persistence** [DEFAULTED] — Is a per-process ephemeral signing key acceptable? Src: analytics_egress wave. Default: ephemeral (generated in run_server; links die on restart — tolerable at ≤24 h TTL); persistent key = one-line ApiConfig wiring in fractalengine/src/main.rs, recorded as an integration request in fe-api/AGENTS.md §share. Undecided: every server restart silently kills all outstanding shared links.
- **D-56 IoT ingestion write path** [DEFAULTED] — Direct db_reader insert vs a DbCommand round-trip through the DB thread. Src: iot_spatial wave D1 (rationale: fe-database/src/AGENTS.md §iot-readings). Default: direct `insert_readings` — safe because iot_reading is append-only with no derived counters, and batches shouldn't queue behind the render loop's channel; validation is all-or-nothing pre-insert but per-row CREATEs can persist a prefix on mid-batch DB error (transactional multi-row INSERT = hardening follow-up). Undecided: violates a strict single-writer policy if one is wanted.
- **D-57 IoT timestamp triple** [DEFAULTED] — Timestamp schema for readings. Src: iot_spatial wave D2. Default: `recorded_at` (RFC-3339 UTC, client-suppliable) + `recorded_at_ms` (i64 epoch-ms, the canonical indexed window/range column) + `hlc_timestamp` (packed HLC, node_log convention) — "both" over the spec's either/or. Undecided: schema churn if trimmed later.
- **D-58 IoT ingestion authz + caps** [DEFAULTED] — Who may ingest, and at what rate. Src: iot_spatial wave D4. Default: Editor+ to ingest (it's a write; exports stay Viewer+), 1000 readings/request, 10 ingest req/s per DID — constants in limits.rs. Undecided: caps are guesses, not product numbers.
- **D-59 node_log cap value** [DEFAULTED] — Default size of the per-node log ring. Src: p2p_unblock_now FR-2. Default: K=1024 (spec said "default small"), clamped ≥1, implemented as Vec + front-drain to keep the serialized EntitySnapshot shape stable for fe-api consumers. Feeds D-7's open retention-vs-sync question. Undecided: value tuned by guess, not by sync-window math.
- **D-68 RBAC tables dropped from egress whitelist** [DEFAULTED] — Should `role`/`verse_member` be readable via /query + exports? Src: 2026-07-15 automated security review (subquery whitelist bypass + elevated DDL substring bypass, both fixed in query_guard.rs). Default: removed from the read whitelist (RBAC data is not BI egress; deny-by-default); the Owner-gated elevated endpoint retains them; every FROM clause (subqueries included) is now whitelist-checked and non-identifier FROM targets are rejected. Undecided: a legitimate membership/role-reporting BI use-case would need its own explicit, scope-filtered surface.

## B. CRS / scale semantics

- **D-9 CRS metadata: block or placeholder** [DEFAULTED] — May CRS-correct egress ship with placeholder CRS metadata, or must it hard-block on hexon_scale FR-1..5? Src: analytics_egress (plan D3), hexon_scale. Default: consume whatever hexon_scale has landed; documented placeholder fallback; never hard-block. Wave (fe-query Phase 1 + analytics_egress): the audit's placeholder string was rejected — shipped marker is `PETAL-LOCAL:meters;origin=unset` (descriptive STRING in the GeoParquet 'geo' metadata, not PROJJSON, not null — never label local meters as EPSG:4326); broader-than-petal /query envelopes use `PETAL-LOCAL:meters;origin=per-petal`; /query/elevated + /analytics/query emit crs None for now; Phase 5 stamps the real terrain-origin string or converts to lat/lon before claiming EPSG:4326. Undecided: serializes the two P0 tracks.
- **D-10 Rulers trail egress GA** [DEFAULTED] — Finish hexon_scale FR-8..10 rulers/callouts UI now, or after BI-egress GA? Src: hexon_scale, roadmap P0 §2. Default: FR-1..5 (scale/GSD/CRS spine) first; rulers trail. Undecided: agent time drains into P2 UI while the P0 spine waits.
- **D-11 Dimension-callout persistence shape** [USER] — Where CAD-style dimension callouts persist in the annotation/property store (hexon_scale OQ-3; plan gates Phase 6 on this). Src: hexon_scale FR-10. Undecided: Phase 6 stays blocked; ad-hoc persistence becomes migration debt.
- **D-60 Scale-bar v1 approximations** [DEFAULTED] — Accept HUD-grade approximations for the v1 scale bar? Src: hexon_scale wave. Default: meters-per-pixel via the down-looking approximation 2·h·tan(fov/2)/viewport_height (not tilt-aware — standard HUD behavior; refine in Phase 5/6 if tilt accuracy matters); 120 px bar anchored bottom-left, egui foreground painter; hides on no enabled terrain config / no single camera / degenerate inputs; RulerSettings carries only show_scale_bar (default on, unpersisted) — Phase 5/6 toggles extend the same resource. Undecided: nothing blocking.

## C. Hexon foundry: open/closed split + track migration

- **D-12 The open/closed line** [USER] — Where exactly does open-core FractalEngine (format + reference tooling) end and the closed foundry/registry/marketplace begin, and when is the separate foundry project created? Src: hexon_delta_format, hexon_p2p_bucket, verse_services, p2p_mycelium_completion, roadmap §2. Roadmap lean (not ratified): open format spec + reference tooling here; closed foundry + official registry + marketplace as separate product. Undecided: blocks every foundry-tagged spec; risks open-sourcing the monetizable half.
- **D-13 Foundry specs stay here as reference** [DEFAULTED] — Do the foundry-tagged specs (delta_format, p2p_bucket, verse_services) remain in this repo as reference until the foundry project exists? Src: same tracks + roadmap go-forward slate. Default: yes, kept here; no migration yet. Undecided: agents may implement foundry scope in the main repo.
- **D-14 p2p_mycelium priority** [DEFAULTED] — Foundry-adjacent P2 (alignment) vs main-repo critical-path P0 (stale metadata); includes whether real iroh-docs replication lands here at all. Src: p2p_mycelium_completion. Default: P2, deferred until the foundry direction is set. Related wave flag (code_review_clippy): fe-sync `TilesetDownloadTracker::start` is never called — may hide a real P2P chunk-download bookkeeping gap; check when that feature is wired. Undecided: conflicting priority fields invite an agent to burn a wave on parked P2P work.
- **D-15 Delta contents: IoT ops in or out** [USER] — Include high-frequency IoT/transform ops in hexon deltas or filter to structural changes; drives registry hosting-cost economics. Src: hexon_delta_format OQ1. Undecided: can't price the marketplace; format churn later.
- **D-16 Non-originating checkpoint compaction** [USER] — May peers other than the originator compact checkpoint ranges (trust-model call)? Src: hexon_delta_format OQ2. Undecided: delta format can't finalize its trust boundary.
- **D-17 Generic asset model cherry-pick** [USER] — Does the IoT/analytics core actually need hexon_p2p_bucket FR-1 (generic asset model), or is it foundry-only? Src: hexon_p2p_bucket (alignment: "cherry-pick only if IoT needs it"); bucket OQs 1–3 deferred to an implementation track — not blocking. Undecided: IoT track may rebuild an asset model the bucket spec already designed.
- **D-18 Verse-service runtime** [USER] — In-process (fe-plugin sandbox) vs separate-binary service runtime. Src: verse_services OQ1. Undecided: foundational architecture fork for all future services.
- **D-19 Relay-as-paid-seeder economics** [USER] — Billing model for service operation (money; explicitly punted — non-blocking now). Src: verse_services. Undecided: needed before the foundry business model closes.
- **D-20 Path-asset W5 op-store scope** [USER] — Is W5 (PetalHexon op-store + multi-asset x-fe-directory) still in scope under BI-first priorities, or deferred/merged into the foundry / p2p_bucket direction? The 2026-07-13 "maximal scope" choice predates the repositioning. Src: hexon_path_asset. Wave: plan preamble now requires the implementer to reconfirm W5 is next-up before Phase 1; FR-6's directory-asset manifest must be reconciled with hexon_p2p_bucket FR-1/FR-2's sketch first so the two tracks don't fork formats. Undecided: stale scope note licenses off-strategy work.

## D. P2P / auth sequencing

- **D-21 auth_policy promotion order** [DEFAULTED] — Roadmap minimal slice (RoleLevelPolicy + fe-hexon RBAC gap + results filtering) first, vs spec's fe-sync write-path gate first. Src: auth_policy_pattern, roadmap P0 §3. Default: minimal slice first; P2P write-path gate stays parked behind analytics egress. Undecided: auth work re-couples to parked P2P.
- **D-22 iroh 1.0 timing** [USER] — Start the 1.x migration now (folding mycelium's reopened phase 1-2 Engine wiring into 1.x) vs keep it calendar-scheduled Q3/Q4 on 0.35; relay EOL 2026-12-31 is the clock. Src: iroh_1_0_upgrade, p2p_mycelium_completion. Note: D-62 added a lockstep-bump obligation to this migration. Undecided: duplicate migration work or an EOL scramble.
- **D-23 Relay posture after EOL** [USER] — Rely on n0's hosted 1.x relays vs commit to the self-hosted relay container as production default (spec leans self-hosted but doesn't decide). Src: iroh_1_0_upgrade. Undecided: ops burden unowned; docker relay deliverable's priority unclear.
- **D-24 Relay WS mutation surface** [DEFAULTED] — Implement EntityCommand/EntityCommandResult (CUD over WS) per spec, or keep mutations REST-only? Src: headless_relay. The audit default (cut) was superseded — wave implemented a minimal surface: the four existing node CUD DbCommands (create_node, delete_node, set_node_property, delete_node_property) with clean DbRequest round-trips; verse/fractal/petal rename/delete intentionally REST-only (avoids duplicating hierarchy-management policy in ws.rs); WS errors are client-safe generics with details in tracing. Undecided: ratify the narrow surface (or extend/cut it).
- **D-25 SSO sequencing** [DEFAULTED] — Schedule SSO after auth_policy lands and not ahead of BI-egress work (wave 4). Src: sso_federation ×2, roadmap alignment (PLATFORM-KEEP P2). Default: deferred behind egress + auth_policy. Undecided: enterprise auth work jumps the analytics queue.
- **D-26 SSO token lifetime** [USER] — 1-hour short-lived vs 30-day parity with native API tokens (spec self-contradicts; security-relevant). Src: sso_federation. Undecided: can't implement Phase 1.
- **D-27 SSO admin UI surface** [USER] — egui dialogs (as spec'd) vs the WebView/Tauri UI direction for Phase 3. Src: sso_federation. Undecided: Phase 3 UI built on the losing platform.
- **D-61 NodeRenamed semantics** [DEFAULTED] — What does RenameEntity emit when EntityType has no Node variant? Src: headless_relay wave (dated note in plan Task 3.2). Default: RenameEntity emits SceneChange::NodeRenamed carrying the renamed entity's (verse/fractal/petal) id; downstream consumers (fe-entity-store, WS clients) no-op on unknown node ids; a dedicated node-rename DbCommand emitting a true node NodeRenamed is future work. Undecided: WS clients receive rename events referencing non-node ids.
- **D-62 BBR via pinned iroh-quinn-proto** [DEFAULTED] — Take a direct iroh-quinn-proto 0.13 dependency now, or defer BBR to iroh_1_0_upgrade? Src: p2p_unblock_now FR-4. Default: direct dep for BbrConfig (iroh 0.35 re-exports TransportConfig/ControllerFactory but not the concrete config types); MUST bump in lockstep with iroh (note appended to iroh_1_0_upgrade metadata); startup log reports the configured controller — config treated as authoritative since quinn exposes no endpoint-level getter. Undecided: a forgotten lockstep bump breaks the 1.x migration.
- **D-63 Replication-bridge drop semantics** [DEFAULTED] — How are bridge-channel send failures classified? Src: p2p_unblock_now FR-1. Default: Disconnected = shutdown, NOT counted as a drop; dropped-on-Full accepted as best-effort loss because the row is already durably committed in SurrealDB before the bridge send; no retry queue. Undecided: silent best-effort loss may surprise sync consumers.

## E. UX / product surface choices

- **D-28 Egress panel home** [DEFAULTED] — Where the copy-for-BI panel lives. Src: analytics_egress (plan D5). Default: GIS panel. Wave: implemented — the card lives in the GIS panel's Export tab (fe-ui/src/panels/gis_panel.rs → egress_card.rs); API base URL defaults to http://localhost:8765, user-editable in the card, no config plumb-through. Undecided: ratify placement or move it.
- **D-29 UX-QA pass + ux_polish scoping** [USER] — The hands-on GUI review is user-owned and must be scheduled/performed by the user; FR-2 scoping of the follow-on ux_polish track is explicitly the user's. Src: ux_qa_review. Undecided: the roadmap's initiative-3 UX track never starts.
- **D-30 Agent-seeded findings** [USER] — May an agent pre-populate findings.md with static-analysis candidates before the user's pass, or is the log purely user-authored? Src: ux_qa_review. Undecided: minor; ambiguity stalls prep work.
- **D-31 Reportable-selection affordances** [USER] — Where the analytics-egress affordance lives in the UI and which spatial selections are reportable (petal / track / bbox; checklist §F). Feeds analytics_egress Phase 4. Src: ux_qa_review. Undecided: copy-paste UX ships to a guess of the workflow.
- **D-32 Inspector scope under repositioning** [USER] — Is FR-3 (petal/fractal/verse inspection) still wanted, and is first FR-4 delivery cut to a read/write role list (deferring invites/presets/audit history)? Src: inspector_settings ×2. Undecided: track scope 3× larger than the strategy needs.
- **D-33 Inspector tab taxonomy** [DEFAULTED] — Keep shipped Properties/ApiAccess/Query + add Access, vs rebuild toward spec'd Info/Settings/Access? Src: inspector_settings ×2. Wave (inspector_settings): FR-2 FOLD taken — spec folded to the shipped tab set; Access lands as a NEW fourth InspectorTab variant; the "Settings" concept is covered by the entity_settings dialog; active_tab intentionally persists across selections (spec OQ 3 resolved by the fold; per-tab sensitive state resets via inspector_sync). Still open [USER] residuals: multi-selection behavior, panel resizability. Undecided: was [USER]; ratify the fold.
- **D-34 BIM FR-5 superseded** [DEFAULTED] — Confirm FR-5 wall-binding is fully superseded by the GPX rip-walls work (commit 1b39af3) and amend the spec. Src: bim_primitives. Default: superseded per alignment note; spec amendment pending. Undecided: spec/reality drift invites re-implementation.
- **D-35 Petal-wide primitive materialization** [USER] — Is selected-only promotion acceptable for v1, or is petal-wide materialization required to call P1 done? Src: bim_primitives. Wave (bim_primitives_on_paths): FR-1 petal-wide path landed via async GetNodeProperties on cold cache — fallback sign appears first, promotes when the fetch lands (fe-runtime hierarchy payload out of territory; truly-immediate cold start would carry the primitive prop in NodeHierarchyData). Question narrows to: is sign-first-then-promote acceptable v1 behavior? Undecided: P1 completion status is ambiguous.
- **D-36 Path-asset stamping end state** [USER] — Is the shipped asset-node/blob:// picker (path_asset_picker v1) the accepted end state, or are hexon-ref semantics (picker mirrors hexon_manager) still wanted? FR-1a browse-new-glb also gated on the user lifting the fe-database ImportGltf quarantine. Src: hexon_path_asset. Wave: hexon_ref-vs-asset-node recorded as spec.md Open Question 1 — decide during/after FR-6. Undecided: FR-1a stays quarantine-blocked; picker direction unsettled.
- **D-37 Sidebar toggle + logging style** [DEFAULTED] — FR-8: remove the sidebar toggle (plan default: remove + TODO) vs keep as auto-collapse override; FR-10: tracing vs documented eprintln! for pre-LogPlugin messages (plan gives guidance). Src: code_review_cleanup ×2. Undecided: trivial; agent proceeds on plan defaults.
- **D-38 Placement surface semantics** [USER] — Click-to-place raycasts to Y=0 plane vs snapping to terrain surface elevation (interacts with the node_placement_z_axis follow-up; relevant even if drag_drop stays deferred). Src: drag_drop_placement. Undecided: inconsistent placement behavior across features.
- **D-64 Mint affordance is a copyable curl** [DEFAULTED] — Should "Get shareable link" be an in-app call? Src: analytics_egress wave. Default: it displays a copyable curl command (format=parquet; edit the copied body for csv) — fe-ui has no HTTP-client machinery to fe-api (only curl-string precedents in inspector.rs / entity_settings.rs). Follow-up: add a fe-ui→fe-api client seam and wire the button to POST /api/v1/query/share directly. Undecided: the flagship copy-paste UX ships one step clunkier than designed.
- **D-65 Primitive-vs-asset render precedence** [DEFAULTED] — Which wins when a node carries BOTH asset_path and a primitive descriptor? Src: bim_primitives_on_paths wave. Default: the GLTF renders (asset wins), consistent with the selection-time reconcile promoting only FallbackSigns; flip in spawn_branch() if primitives should override; petal-respawn property fetches are issued only for asset-less cache-miss nodes. Undecided: silent precedence surprises authors stacking both.

## F. Deferred-track disposition (kill / keep / merge)

- **D-39 drag_drop → UX track** [DEFAULTED] — Fold the track into the user's UX track per alignment (DEFER/hand-off); if revived, decide whether OS-file-drop still matters vs only placement-preview/click-to-place (FR-4/5/6). Src: drag_drop_placement ×2. Default: defer/hand off; no agent pickup. Undecided: agents may resume a superseded track.
- **D-40 light_box: archive or shelve** [USER] — Revive this P3 track at all vs archive under the analytics repositioning; its three OQs are contingent on revival. Src: light_box ×4. Undecided: dead track occupies the board.
- **D-41 profile_manager stays deferred** [DEFAULTED] — Keep OFF-STRATEGY P3 per alignment; OQs contingent on revival. Src: profile_manager ×4. Default: deferred, no agent starts it. Undecided: none while verdict stands.
- **D-42 Tauri spike: NO-GO or shelved-open** [USER] — Formally close the spike (time-box expired + roadmap DEFER) vs leave shelved-but-open; closing forecloses the desktop-shell-inversion option rated "viable with 2–4 weeks work". Src: tauri_host_shell_spike. Undecided: ambiguous spike status.
- **D-43 Splat LOD fix: revive?** [USER] — Still want the low-resolution splat fix (splat_desired_zoom / LOD zoom-selection)? Follow-up was reverted; roadmap marks splat work OFF-STRATEGY P3. Src: terrain_splat_view. Undecided: visible visual-quality wart persists by policy.
- **D-44 Splat spec amendment** [DEFAULTED] — Drop unshipped FR-4 niceties (splat_ready flag, bake_splats example, gis-tile-etl bake stage) and amend the spec to the shipped design. Src: terrain_splat_view. Default: amend, not debt. Undecided: phantom debt on the books.
- **D-45 Path-asset track split** [DEFAULTED] — Execute the alignment-pass split: W3 glb-render (P1) and W5 op-store (P2) as separate tracks. Src: hexon_path_asset. Default: split as called for. Undecided: one track carries two priorities.
- **D-46 Download-fix verification gate** [USER] — User must confirm in the running app that the Download button behaves (native save dialog; untestable by automation) — the only gate before completed+archive. Src: asset_download_fix. Undecided: finished track lingers open.
- **D-47 Onboarding wizard disposition** [USER] — Is the wizard UX (FR-1..3) wanted at all under the analytics repositioning; if so it moves to the user's UX track. Src: seedling_onboarding. Undecided: track can't archive or migrate.
- **D-48 Entity delete/rename CRUD** [USER] — Is verse/fractal/petal/room delete/rename in-scope for the analytics strategy? A data-layer gap, not wizard UX. Src: seedling_onboarding. Note: headless_relay's RenameEntity/WS work (D-24/D-61) touches the same surface — coordinate. Undecided: a real CRUD gap hides inside a deferred UX track.
- **D-49 petal_seed archive** [DEFAULTED] — Archive proceeds; the minor OS drag-drop-of-.glb sugar question defers to the UX track. Src: petal_seed. Default: archive now. Undecided: nothing blocking.

## G. Release / CI platform coverage

- **D-50 macOS in scope at all?** [USER] — Is macOS support required for the spatial-analytics/BI repositioning, or is FR-2 downgraded/deferred (no hardware or CI job exists; alignment says PLATFORM-KEEP P2)? Src: cross_platform_desktop. Undecided: platform promise nobody can verify.
- **D-51 Artifact-matrix scope + timing** [USER] — Slim release_ci to Linux artifacts + Docker/GHCR relay (deferring the 8-target matrix: macOS universal, Windows ARM64), and decide whether macOS/ARM64 verification jobs land in cross_platform's workflow now or wait for the matrix. Src: release_ci, cross_platform_desktop. Undecided: CI-minute spend and track sequencing unsettled.
- **D-52 Relay runtime image** [DEFAULTED] — Keep shipped `alpine:latest` runtime (shell + ca-certs, debuggable) vs the spec's FROM-scratch for the published GHCR image. Src: release_ci. Default: alpine as shipped. Wave: blessed via dated spec amendment (FR-4 deviation) — done pending ratification. Undecided: spec/artifact mismatch.
- **D-53 Publishing gate** [USER] — GHCR push + GitHub Releases need repo settings/tokens and a live v0.0.0-ci-test tag only the user can authorize/perform (Task 2.7). Src: release_ci. Undecided: release pipeline unverifiable end-to-end.
- **D-66 CI spec deviations (dated amendments)** [DEFAULTED] — Ratify the wave's blessed deviations? Src: release_ci + cross_platform waves. Default: rust-cache (Swatinem v2) over sccache (FR-3; sccache is NOT being added); single SHA256SUMS manifest in the publish job instead of per-artifact .sha256 files; scoped test sweep (fe-ui, fe-terrain, fractalengine) over `cargo test --workspace` (FR-1 note), mirrored on macOS. Undecided: ratify or revert to spec.
- **D-67 FE_KEYSTORE_BACKEND: wire or drop** [DEFAULTED] — The spec-mandated env var is read nowhere in Rust source (tests use InMemoryBackend; relay secrets come from FE_SECRET_{SERVICE}_{ACCOUNT} via EnvBackend). Src: release_ci wave. Default: set in CI test env + Dockerfile per spec with comments marking it informational. Follow-up: either wire actual backend selection to the var or drop it from specs. Undecided: dead config invites false confidence.

## H. OSS release + crate consolidation (appended 2026-07-17)

Sources: 2026-07-17 OSS-release audit (REL-01..REL-11, feeds track `oss_release_20260717`) + crate-consolidation audit (F1–F9; executed portions recorded in [../../decisions/crate-consolidation-20260717.md](../../decisions/crate-consolidation-20260717.md)).

- **D-69 OSS license: MIT OR Apache-2.0 dual** [DEFAULTED] — Which license does FractalEngine ship under? Src: OSS audit REL-01/REL-02 (no LICENSE file existed while README claimed "All rights reserved. See LICENSE for details"; zero crates carried license metadata). Default: Rust-convention dual **MIT OR Apache-2.0**, scaffolded 2026-07-17 (LICENSE-MIT + LICENSE-APACHE at root, `[workspace.package] license = "MIT OR Apache-2.0"`, THIRD-PARTY-LICENSES.md + deny.toml with a surrealdb BUSL-1.1 exception). NOT ratified — no public push, tag, or crates.io publish until this resolves; a license choice is effectively irreversible once code ships under it. Undecided: the repo carries license files the owner never actually chose.
  **RATIFIED 2026-07-17 (user): Apache-2.0 single license** — user chose Apache over the scaffolded dual default; LICENSE-MIT removed, `[workspace.package] license = "Apache-2.0"`, README/CONTRIBUTING/THIRD-PARTY updated same day.
- **D-70 conductor/ bundle public vs private on OSS release** [USER] — Does the conductor/ PM bundle (including this unratified register's internal deliberations) + research/ ship in the public repo, or move to a private sibling before first push? Src: OSS audit REL-10. If public: add a conductor/README.md framing it as open project management and mark this register draft/unratified. If private: split before the first push. Undecided: internal deliberations publish by default on first push.
  **RATIFIED 2026-07-17 (user): conductor/ stays public** — shipped as open project management; conductor/README.md added framing the bundle and marking this register's undecided entries as draft deliberations.
- **D-71 fe-network stage-2 retirement** [USER] — Move `AssetId` (+ `GossipMessage`) into `fe-runtime::messages`, retire the libp2p kademlia swarm + network thread, and delete the fe-network crate (dropping libp2p 0.56 from every workspace build) — or keep fe-network as a seam for a possible swarm revival? Src: crate-consolidation F1 (executed portions + mechanical surface in the 2026-07-17 decision record). Stage-1 dead-dep prune already applied 2026-07-17 (never-imported iroh-blobs/iroh-gossip/iroh-docs removed from fe-network/Cargo.toml; libp2p and the crate untouched). Board evidence says the live P2P direction is iroh (iroh_1_0_upgrade), but foundry-candidate specs (hexon_p2p_bucket "handshake-then-swarm", p2p_mycelium_completion) might intend a swarm revival. Undecided: a 190-line stub crate keeps libp2p in every build and the architecture docs claim a thread that only answers Ping/Pong.
  **RATIFIED 2026-07-17 (user): KEEP fe-network — no retirement.** User directive: "we need peer to peer, it's the main differentiator." P2P is core product identity; the crate stays as the swarm seam and libp2p remains a workspace dependency.
- **D-72 First-run UI vocabulary** [USER] — Keep the verse/fractal/petal jargon in first-run and user-facing UI, or shift to domain terms an OSS newcomer parses on sight? Src: OSS audit front-door review; feeds the user-owned UX track (roadmap defers UX to the user — no agent pre-emption; baked terminology rules already fix "path"/"map"/"Portal URL" independently of this call). Undecided: first-run comprehension for OSS newcomers vs the product's established identity.
  **RATIFIED 2026-07-17 (user): KEEP the verse/fractal/petal vocabulary** ("for sure"). The hierarchy jargon is canon in all user-facing UI; remaining polish is plain-language framing (explainers), never renaming.

## Quick ratification checklist ([DEFAULTED] — one-line yes/no each)

1. D-1: parquet+CSV download (DuckDB-httpfs-readable) primary; OData fast-follow; no native wire; v1 exports node-table-only — OK?
2. D-2: shareable queries = short-TTL ed25519-signed scoped URLs (≤86400 s); parquet/csv mints need a petal-scoped issuer token — OK?
3. D-3: caps 10k rows/8 MiB (`/query`), 500k rows/128 MiB (exports); cap-exceeded errors, never truncates — OK?
4. D-4: scope-ceiling/export-filter slice in analytics_egress; full RBAC engine later in auth_policy — OK?
5. D-6: IoT v1 ingestion = push-style authenticated HTTP batch endpoint (DID token); polling connectors later — OK?
6. D-9: CRS marker `PETAL-LOCAL:meters;origin=unset` (never mislabel as EPSG:4326); egress never hard-blocks on hexon_scale — OK?
7. D-10: hexon_scale FR-1..5 now; rulers/callouts UI after egress GA — OK?
8. D-13: foundry specs stay in this repo as reference until the foundry project exists — OK?
9. D-14: p2p_mycelium is P2 foundry-adjacent (fix its stale P0 metadata) — OK?
10. D-21: auth_policy minimal slice first; fe-sync write-path gate stays parked — OK?
11. D-24: relay WS mutations = the four node CUD commands only; verse/fractal/petal rename/delete stay REST-only — OK?
12. D-25: SSO waits for auth_policy and BI egress (wave 4) — OK?
13. D-28: copy-for-BI card lives in the GIS panel's Export tab — OK?
14. D-33: inspector keeps shipped Properties/ApiAccess/Query + new Access tab; tab persists across selections — OK?
15. D-34: BIM FR-5 superseded by rip-walls; amend spec — OK?
16. D-37: remove sidebar toggle + tracing-or-documented-eprintln per plan — OK?
17. D-39: drag_drop folds into the UX track; no agent pickup — OK?
18. D-41: profile_manager stays deferred P3 — OK?
19. D-44: amend splat spec to shipped design; drop FR-4 niceties — OK?
20. D-45: split hexon_path_asset into W3 (P1) and W5 (P2) tracks — OK?
21. D-49: archive petal_seed; drag-drop sugar goes to the UX track — OK?
22. D-52: keep alpine runtime image for the published relay (amendment recorded) — OK?
23. D-54: shipped GeoParquet stack + column contract (arrow 54.x, WKB Point Z, 6×f32 rot/scale, no node_log, JSON-string properties) is the v1 export schema — OK?
24. D-55: per-process ephemeral share-signing key (links die on restart) acceptable until ApiConfig wiring — OK?
25. D-56: IoT ingestion writes directly via db_reader (no DbCommand round-trip) — OK?
26. D-57: timestamp triple recorded_at + recorded_at_ms (canonical) + hlc_timestamp — OK?
27. D-58: IoT ingest = Editor+, 1000 readings/request, 10 req/s per DID — OK?
28. D-59: node_log cap default K=1024 — OK?
29. D-60: v1 scale bar = down-looking approximation, 120 px bottom-left, show_scale_bar only — OK?
30. D-61: RenameEntity emits NodeRenamed with the entity id; true node-rename DbCommand deferred — OK?
31. D-62: direct iroh-quinn-proto 0.13 dep for BBR, lockstep-bumped with iroh — OK?
32. D-63: bridge Disconnected = shutdown (not a drop); Full-drop = accepted best-effort loss, no retry queue — OK?
33. D-64: shareable-link mint ships as a copyable curl; fe-ui→fe-api client seam is a follow-up — OK?
34. D-65: asset_path wins over primitive descriptor when a node has both — OK?
35. D-66: CI amendments (rust-cache, single SHA256SUMS, scoped test sweep) — OK?
36. D-67: FE_KEYSTORE_BACKEND stays informational pending wire-or-drop — OK?

37. D-68: role/verse_member removed from the /query + export whitelist (elevated endpoint keeps them); all FROM clauses whitelist-checked — OK?

38. D-69: dual MIT OR Apache-2.0 license (scaffolded 2026-07-17) — ~~OK?~~ **RESOLVED 2026-07-17: user chose Apache-2.0 single (see D-69 above).**

## How to resolve

Edit the entry in place: append a dated resolution line (`Resolved 2026-07-NN: <ratified/overridden/killed — note>`) and flip its tag to **[RESOLVED]**. Checklist "yes" = a one-line ratification on the entry. When every entry is resolved, set metadata.json status to `completed` and archive the track. UX-theme entries (D-28..D-38, D-64, D-65) are mirrored one-line into `../ux_qa_review_20260714/findings.md`; this file is the authoritative copy.
